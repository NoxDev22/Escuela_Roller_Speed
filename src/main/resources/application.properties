spring.application.name=roller-speed

# --- CONFIGURACIÓN DE MYSQL (ACTIVA) ---
spring.datasource.url=jdbc:mysql://localhost:3306/roller_speed?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Maiguel201@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- CONFIGURACIÓN DE JPA / HIBERNATE ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- BASE DE DATOS H2 (DESACTIVADA) ---
spring.h2.console.enabled=false